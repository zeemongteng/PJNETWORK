[project]
name = "void-protocol-backend"
version = "1.0.0"
description = "GBNL Void Protocol - Data Sharding Security Model"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "pydantic>=2.0.0",
]
